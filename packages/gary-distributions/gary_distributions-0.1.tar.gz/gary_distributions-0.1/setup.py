from setuptools import setup

setup(name='gary_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['gary_distributions'],
      zip_safe=False)
