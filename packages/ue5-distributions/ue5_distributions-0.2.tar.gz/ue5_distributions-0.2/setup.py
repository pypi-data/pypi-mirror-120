from setuptools import setup

setup(name='ue5_distributions',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['ue5_distributions'],
      zip_safe=False)
