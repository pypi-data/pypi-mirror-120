apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blast-dbs-pvc
spec:
  storageClassName: "gcp-pd"
  accessModes:
    - ReadOnlyMany
    - ReadWriteOnce
  resources:
    requests:
      storage: 1000G
