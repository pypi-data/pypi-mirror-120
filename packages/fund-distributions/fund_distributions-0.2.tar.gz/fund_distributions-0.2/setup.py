from setuptools import setup

setup(name='fund_distributions',
      version='0.2',
      description='Gaussian  and Binomial distributions',
      packages=['fund_distributions'],
      author='Eduardo Chiarotti',
      zip_safe=False)
