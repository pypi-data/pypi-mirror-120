from setuptools import setup

setup(name='prob_bin_gauss_distributions',
      version='0.1',
      description='Probability distributions',
      packages=['prob_bin_gauss_distributions'],
      zip_safe=False)
