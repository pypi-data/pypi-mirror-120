---
environments:
  production:
    host: yourhost.com
    scheme: https
    port: 443
    credentials: ~/.tin/apis/kace/production.yml
    auth_type: none
    use_session: true
    ssl:
      verify: true
    api_file: kace-api.yml
common:
  # Common settings apply to all environments
  type: "application/json"
  basepath: "/api"
  params:
    may: ["links", "filter_by", "filter_value"]
