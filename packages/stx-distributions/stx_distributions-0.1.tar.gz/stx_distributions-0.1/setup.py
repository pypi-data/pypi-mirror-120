from setuptools import setup

setup(name='stx_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['stx_distributions'],
      zip_safe=False)
