from setuptools import setup

setup(name='distributions_test_RCR',
      version='0.1',
      description='Gaussian distributions, binomial distributions',
      packages=['distributions_test_RCR'],
      zip_safe=False)
