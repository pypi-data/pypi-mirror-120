from setuptools import setup

setup(name='distributions_of_learning',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_of_learning'],
      zip_safe=False)
