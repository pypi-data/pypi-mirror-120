#!/usr/bin/env python3
import jinja2
import json
import sys

env = json.load(open(sys.argv[1]))
template = open(sys.argv[2]).read()

t = jinja2.Environment().from_string(template)
print(t.render(**env))
