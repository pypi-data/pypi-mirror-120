from setuptools import setup

setup(name='gaussian_distributions_hfg',
      version='0.1',
      description='Gaussian distributions',
      packages=['gaussian_distributions_hfg'],
      zip_safe=False)
